{
  "name": "hirenow",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/**/*.ts": [
      "prettier --write",
      "eslint --fix"
    ],
    "frontend/**/*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ]
  }
}
